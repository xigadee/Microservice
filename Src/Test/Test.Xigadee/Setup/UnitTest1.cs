using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Xigadee;

namespace Test.Xigadee.Setup
{
    [TestClass]
    public class Setup
    {
        [TestMethod]
        public void Setup1()
        {
            var pipeline = new MicroservicePipeline(nameof(Setup1));

            //var hello = SimpleTestClass.Doesthiswork();
            //pipeline
            //    .AdjustPolicyTaskManager((t,c) => t.
        }
    }
}
